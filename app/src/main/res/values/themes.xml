<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.StatsForChesscom" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.StatsForChesscom" parent="Base.Theme.StatsForChesscom" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenIconBackgroundColor">@color/design_default_color_background
        </item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_knight</item>

        <item name="android:windowTransitionBackgroundFadeDuration">200</item>
        <!--        <item name="android:windowBackground">@drawable/ic_knight</item>-->
        <!-- Set the theme of the Activity that directly follows your splash screen -->
        <item name="postSplashScreenTheme">@style/Theme.StatsForChesscom</item>
    </style>

</resources>