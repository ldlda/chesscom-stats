<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- AppBarLayout with collapsing SearchView -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        app:elevation="0dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="12dp"
            android:gravity="center"
            android:text="@string/plr_search"
            android:textSize="24sp"
            app:layout_scrollFlags="scroll|enterAlwaysCollapsed" />

        <!-- SearchView - collapses but enters on scroll up -->
        <androidx.appcompat.widget.SearchView
            android:id="@+id/player_search_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/search_bg"
            android:iconifiedByDefault="false"
            android:queryHint="@string/plr_search"
            app:iconifiedByDefault="false"
            app:layout_scrollFlags="scroll|enterAlways" /> <!-- magic here -->

    </com.google.android.material.appbar.AppBarLayout>

    <!-- RecyclerView - scrolls and triggers collapse -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/results_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>